# top-most EditorConfig file
root = true

# (defaults) applies to all files
[*]
charset = utf-8
end_of_line = lf
indent_size = 2
indent_style = space
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
# avoid accidentally removing significant white space
trim_trailing_whitespace = false
